# Completion Evidence

Add one folder per exercise and keep only public, reviewable evidence here. Each screenshot should show the feature name, selected network, transaction or payment identifier, and final successful state. Pair every image with its transaction hash or payment hash in `DEVLOG.md`.

Do not commit private keys, seed phrases, cookies, bearer tokens, database credentials, or wallet recovery material.
